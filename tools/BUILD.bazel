load("@rules_python//python:defs.bzl", "py_binary")

package(default_visibility = ["//visibility:public"])

py_binary(
    name = "split",
    srcs = ["split.py"],
    deps = [
        "@pip//pyyaml",
    ],
    main = "split.py",
)
